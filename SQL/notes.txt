in operator - It is a multivalued operator which is used to accept multiple values at rhs.
it is similar to equals operator it returns true if it satisfies any one of the values present at rhs

not in - similar to in operator but instead of accepting values it will reject the values 

between operator - it is used whenever it has range of values it works by including the given range

we cannot interchange the range of values

write a program  to display details of employees with half term salary if they are earining in the rage or 1000 to 
5000 but they are not working as salesman or analyst


